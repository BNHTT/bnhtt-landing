
  # Convert templates to code

  This is a code bundle for Convert templates to code. The original project is available at https://www.figma.com/design/IBAEHmEr9oVtCx5osDLIhN/Convert-templates-to-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  